
                      $("section#step-1").html(<%=# render 'interventions-form/step-1' %>);


                      $.ajax({
                        url: "test.html",
                        context: document.body
                      }).done(function() {
                        $( this ).addClass( "done" );
                      });

